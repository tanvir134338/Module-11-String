/**
 * Three type of variable we have decleared.
 */
 const goals = 4;
 const player = cr7;
 const isGolden = true; 

 //condition----------
 if(player==='mbappe'){
    console.log('ki khawyaise tor bap e ')
 }

 const mathes = ['vs algeria','vs portugal']